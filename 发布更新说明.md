# 发布更新说明

日常开发继续使用 `/Users/Apple/Downloads/样片工厂`。GitHub 同步使用独立发布仓库 `/Users/Apple/Downloads/AI人脸提示词/TTyangpian-release`，这样不会误传正在生成的图片、任务记录、缓存、日志或密钥。

## 同步最新代码

```bash
cd /Users/Apple/Downloads/AI人脸提示词/TTyangpian-release
./scripts/sync_from_live.sh --message "fix: 简述本次修改"
git push origin HEAD:main
```

同步脚本采用明确白名单；`app.py` 使用三方合并，避免日常业务修改覆盖 Windows 和自动更新代码，并在提交前自动执行隐私检查和全部测试。不要用运行目录覆盖 `desktop_app.py`、`requirements.txt`、`version.json`、`packaging/`、`.github/` 或 `default_data/`，这些文件包含跨平台发布配置和脱敏模板。

## 让安装用户收到更新

推送源码和发布安装包是两件事。已安装用户的“检查更新”读取 GitHub Releases，因此发布新版本时还要：

1. 把 `version.json` 递增为新版本号；
2. 提交并推送代码；
3. 创建并推送同版本标签，例如 `git tag v1.5.1` 和 `git push origin v1.5.1`；
4. 等待 GitHub Actions 构建 Windows 与 macOS 资产；
5. 在 GitHub Release 页面核对安装包和 SHA-256 校验文件。

只有 Release 构建成功后，软件内更新才具备可下载的安装文件。
