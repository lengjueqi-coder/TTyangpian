# 样片工厂 v1.5.0 使用说明

## Windows（推荐安装包）

1. 从 GitHub Release 下载 `sample-factory-v1.5.0-windows-x64-setup.exe`。
2. 双击安装。因为安装包暂未购买商业代码签名证书，Windows 可能显示“未知发布者”；请确认下载地址属于本项目后继续。
3. 从开始菜单或桌面快捷方式启动“样片工厂”。
4. 用户配置、素材、模型、日志与生成任务保存在 `%LOCALAPPDATA%\样片工厂`，安装升级不会覆盖这些内容。

## macOS 安装包

1. 下载与 Mac 架构对应的 DMG。
2. 将“样片工厂.app”拖入“应用程序”。
3. 首次启动如被 Gatekeeper 拦截，请右键应用并选择“打开”。
4. 用户数据保存在 `~/Library/Application Support/样片工厂`。

## macOS 源码版

1. 双击 `启动.command`。
2. 首次运行会在 `~/Library/Application Support/样片工厂/venv` 安装 Python 依赖。
3. 终端窗口必须保持打开；浏览器会自动访问本地服务。

## 更新

- 点击软件顶部“检查更新”。
- 源码版会下载 `source.zip`，强制校验 SHA-256 后替换程序文件，同时保留 `data`、用户图库、日志、模型、运行缓存和成品。
- Windows 安装版会下载并启动 Windows 安装器；macOS 安装版会下载并打开新 DMG。
- v1.4.0 的旧 DMG 自更新实现不完整，首次升级到 v1.5.0 需要手动下载新安装包；从 v1.5.0 起才使用新的平台化更新流程。

## 数据导入导出

- 顶部“导出”可以生成数据 ZIP。
- 在另一台电脑通过“导入”恢复配置与素材。
- 请勿把 API Key、用户 `data/`、生成图、日志或缓存提交到 GitHub。
